## This file autogenerated by BinaryProvider.write_deps_file().
## Do not edit.
##
## Include this file within your main top-level source, and call
## `check_deps()` from within your module's `__init__()` method

if isdefined((@static VERSION < v"0.7.0-DEV.484" ? current_module() : @__MODULE__), :Compat)
    import Compat.Libdl
elseif VERSION >= v"0.7.0-DEV.3382"
    import Libdl
end
const libbayesopt = joinpath(dirname(@__FILE__), "usr/lib/libbayesopt.so")
const libnlopt = joinpath(dirname(@__FILE__), "usr/lib/libnlopt.so")
function check_deps()
    global libbayesopt
    if !isfile(libbayesopt)
        error("$(libbayesopt) does not exist, Please re-run Pkg.build(\"BayesOpt\"), and restart Julia.")
    end

    if Libdl.dlopen_e(libbayesopt) in (C_NULL, nothing)
        error("$(libbayesopt) cannot be opened, Please re-run Pkg.build(\"BayesOpt\"), and restart Julia.")
    end

    global libnlopt
    if !isfile(libnlopt)
        error("$(libnlopt) does not exist, Please re-run Pkg.build(\"BayesOpt\"), and restart Julia.")
    end

    if Libdl.dlopen_e(libnlopt) in (C_NULL, nothing)
        error("$(libnlopt) cannot be opened, Please re-run Pkg.build(\"BayesOpt\"), and restart Julia.")
    end

end
