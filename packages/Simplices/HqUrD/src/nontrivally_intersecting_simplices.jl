include("simplexoperations.jl")

"""
    `nontrivial_simplexpair()`

Genereate a set of non-trivially intersecting simplices (i.e. they don't intersect along boundaries or vertices only).
"""
function nontrivial_simplexpair()

end
